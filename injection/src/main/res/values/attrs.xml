<?xml version="1.0" encoding="utf-8"?>
<resources>

    <color name="default_color">#00000000</color>
    
    <!-- 底色 -->
    <attr name="normal_color" format="color" />
    <!-- 按下后的颜色 -->
    <attr name="pressed_color" format="color" />
    <!-- 选中后的颜色 -->
    <attr name="checked_color" format="color" />
    
    <!-- 常规状态下-->
    <attr name="normal_drawable" format="reference" />
    <!-- 按下/获得焦点 -->
    <attr name="pressed_drawable" format="reference" />
    <!-- 选中时 -->
    <attr name="checked_drawable" format="reference" />

    <!-- 边界描边的颜色 -->
    <attr name="stroke_color" format="color" />
    <!-- 外界描边的宽度 -->
    <attr name="stroke_width" format="dimension" />
    <!-- 按下后的描边 -->
    <attr name="pressed_stroke_color" format="color" />
    <attr name="pressed_stroke_width" format="dimension" />
    <!-- 选中后的描边 -->
    <attr name="checked_stroke_color" format="color" />
    <attr name="checked_stroke_width" format="dimension" />
    
    <!-- 是否是自动计算的 -->
    <attr name="isSmart" format="boolean" />

    <!-- 是否将图片设置到src中 -->
    <attr name="isSrc" format="boolean" />
    
    <declare-styleable name="SelectorInjection">
        <attr name="normal_color"/>
        <attr name="pressed_color"/>
        <attr name="checked_color"/>
        <attr name="normal_drawable"/>
        <attr name="pressed_drawable"/>
        <attr name="checked_drawable"/>
        <attr name="stroke_color"/>
        <attr name="stroke_width"/>
        <attr name="pressed_stroke_color"/>
        <attr name="pressed_stroke_width"/>
        <attr name="checked_stroke_color"/>
        <attr name="checked_stroke_width"/>
        <attr name="isSmart"/>
        <attr name="isSrc"/>
    </declare-styleable>
   
</resources>

